<?
//Available plugins from folder ~/plugins. The names must be in lower case like 'news_comments', 'products'.
//The class of the plugin must be in folder ~/plugins.
//SQL table of plugin(if needed) must have the same name as model class in lower case.

$mvActivePlugins = array();
?>